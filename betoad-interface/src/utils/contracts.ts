import { AssetInfo, NativeToken, Token } from "types/asset";

export const contractToAddress = (contract: string): string => {
  // if (CONTRACTS[contract]) {
  //   return CONTRACTS[contract].contractAddresses.default;
  // }
  // return contract;
  return "";
};

export const addressToContract = (address: string): string => {
  // const result = Object.keys(CONTRACTS).filter(
  //   (key) => CONTRACTS[key].contractAddresses.default === address
  // );
  // return result[0] || address;
  return "";
};
